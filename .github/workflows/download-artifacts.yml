name: Cypress Tests

on: push

jobs:
  run-cypress:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Installer les dépendances avec npm install (créera package-lock.json automatiquement)
      - name: Install dependencies
        run: npm install
        working-directory: ./cypress

      # Run Cypress tests using official GitHub Action (compact)
      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        with:
          working-directory: ./cypress
          spec: "cypress/e2e/rds.cy.js"
          browser: chrome
          headed: false        # headless
          record: false
          publish-summary: true  # affiche tableau summary dans GitHub
        continue-on-error: true

      # Upload screenshots
      - name: Store screenshots
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: ./cypress/cypress/screenshots

      # Upload videos
      - name: Store videos
        uses: actions/upload-artifact@v4
        with:
          name: cypress-videos
          path: ./cypress/cypress/videos
